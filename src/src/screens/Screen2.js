import React from 'react'
import SlideIn from '../ui/SlideIn'

const Screen2 = props => (

  <SlideIn bg="turquoise" in={props.in}>
    <h1>Screen 2</h1>
  </SlideIn>

)

export default Screen2